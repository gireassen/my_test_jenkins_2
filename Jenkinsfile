pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building development branch: ${env.GIT_BRANCH}"
                // сборка, линт, тесты
                // empty commit
                // empty commit #2
                // empty commit #3
                sh 'echo "Running tests..."'
            }
        }
    }
}
